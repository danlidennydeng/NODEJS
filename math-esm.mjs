const add = (a, b) => {
  return a + b;
};

const subtract = (a, b) => {
  return a - b;
};

export default {
  add,
  subtract,
};

// export default (a, b) => {
//   return a + b;
// };

// const add = (a, b) => {
//     return a + b;
//   };

//   export default add;
